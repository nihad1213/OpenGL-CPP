# OpenGL With CPP

This repository is a basic test project for experimenting with **OpenGL** using C++.  
It includes simple examples such as creating a window, drawing basic shapes, and testing the rendering pipeline.
